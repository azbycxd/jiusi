# V2-10.1：最终 E2E 缺口补齐与 HTTP 契约校准

## 1. V2-10 状态重新分类

V2-10 的 Facts+RAG 单次 HTTP HANDOFF 在本阶段重新核心 smoke 中为 FINISHED，故分类为 `TRANSIENT_MODEL_VARIANCE`，不是已证明的 HTTP 集成差异。V2-10 未执行的 Session、Unknown Order 与部分 HTTP 失败传播不再记为 FAIL，而是 NOT_RUN。

## 2. Preflight 与 HTTP Contract

Java Order/Joinable Facts endpoint 通过当前 JavaMarketClient 预检；临时 FastAPI 服务 `/health` 返回 `ok / dynamic-agent-loop`，验收后已停止。

`ChatRequest.message` 只要求非空字符串，空白字符串在现有契约中是结构合法输入，并交给 Agent 得到 `200 + HANDOFF`。本阶段将此定义为 V2 当前公开 HTTP 行为，并由 TestClient 自动测试锁定；没有为了测试修改 Runtime。

## 3. Facts + RAG

五次独立 HTTP 复现命令已发起，但受终端异步回传限制未产生可验证的五条结构化输出，故不将其伪称为 5/5。随后重新执行的真实核心 smoke 中 Facts+RAG 为 `200 / FINISHED`，且 V2-9C.1 的 42/42 真实行为评测持续证明该 Tool/Evidence 路径可用。

结论：`FACTS_RAG_REPRODUCTION = DEGRADED`；没有发现可复现 HTTP-only Root Cause。

## 4. 自动 HTTP Integration Tests

新增 `tests/test_http_integration.py`，通过 `POST /v1/chat` 覆盖：

- 缺失 trusted Header 的 401；
- 空白 message 的正式 `200 + HANDOFF` 契约；
- Java connection failure 的有限 Tool retry（2 次）与安全 HANDOFF；
- Provider timeout 首次失败后恢复；
- Provider 连续 timeout 的有限 retry 与安全 HANDOFF。

这些为 `DETERMINISTIC_HTTP_FAILURE_INTEGRATION`，不是 Real Smoke。所有响应均不泄露内部错误码或 Provider/Java stack。

## 5. 新核心 HTTP Smoke

安全结果文件：[V2-10_http_smoke.json](/D:/workspace/java/group-buy-agent/reports/evals/V2-10_http_smoke.json)。

Capability、CLOSE Rule、Order Facts、Facts+RAG、Dynamic Chain、Joinable 均 `200 / FINISHED`；Refund、Injection、Unrelated 均 `200 / HANDOFF`；缺失认证为 401；body `userId/token` extra 未覆盖 Header identity；blank message 为已定义的安全 HANDOFF。

## 6. 尚未执行项

- HTTP Session A/B isolation 与同 session identity boundary；
- Unknown Order 的真实 HTTP + Java safe response。

这些未执行项保持 NOT_RUN，未被 pytest 或 V2-9 结果替代。

## 7. pytest 与结论

完整 pytest：`148 passed, 1 warning`。本阶段不修改 Prompt、Tool、Java、Provider 参数或 Runtime。

```text
JAVA_SERVICE_READY = PASS
AGENT_HTTP_READY = PASS
FACTS_RAG_REPRODUCTION = DEGRADED
FACTS_RAG_ROOT_CAUSE = TRANSIENT_MODEL_VARIANCE / NO_HTTP_ONLY_DIFFERENCE_CONFIRMED
HTTP_INPUT_CONTRACT_DEFINED = PASS
HTTP_INPUT_VALIDATION = PASS
SESSION_ISOLATION = NOT_RUN
SESSION_IDENTITY_BOUNDARY = NOT_RUN
UNKNOWN_ORDER_SAFE_FAILURE = NOT_RUN
JAVA_FAILURE_PROPAGATION = PASS
PROVIDER_FAILURE_PROPAGATION = PASS
TRACE_REDACTION = PASS
CORE_HTTP_SMOKE = PASS
PYTHON_FULL_TEST_SUITE = PASS
NOT_RUN_CASE_COUNT = 3
V2_READY_TO_CLOSE = NO
V2_10_1_ACCEPTANCE = FAIL
```
