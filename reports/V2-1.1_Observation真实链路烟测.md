# V2-1.1：Observation 真实链路烟测

## 执行范围

本次是只读真实端到端烟测：未新增功能、未修改运行时代码、Prompt、Java 项目或 `.env`。使用指定 Conda 环境 `group-buy-agent`、既有 OpenAI-compatible Provider、既有 Java 服务和既有授权测试身份执行。

未记录 API Key、认证 Header、可信身份标识、完整 Prompt、完整 Provider 原始响应或 Java 堆栈。

## A. 无需 Tool 的能力咨询

输入：`你能帮我做什么？`

- 真实模型：`gpt-5.5`
- Model Decision：`ANSWER`
- 最终状态：`FINISHED`
- `tool_call_count=0`
- `model_call_count=1`
- 未产生 Observation 或 ToolResult。

## B. 真实订单 Observation 闭环

输入：`为什么我的订单 644398015396 还没有拼团成功？`

真实 Trace 顺序：

```text
ROUTED
-> MODEL_DECISION(CALL_TOOL)
-> TOOL_CALL(get_order_facts)
-> TOOL_RESULT(success=true)
-> MODEL_DECISION(ANSWER)
-> FINISHED
```

结果：

- 第一次真实模型决策：`CALL_TOOL(get_order_facts)`，参数只含 `outTradeNo`。
- Java Tool：真实调用成功，`source=java_market`，`tool_call_count=1`，无 Tool error code。
- Observation：生成 1 条，`tool_name=get_order_facts`；其 data 是已通过 `OrderFacts` Pydantic Contract 的规范化订单、拼团、活动和引用数据。
- 第二次真实模型决策：`ANSWER`，最终状态 `FINISHED`，`model_call_count=2`。
- 模型与工具均无重试。

### 真实 Provider 载荷验证

在同一条真实订单调用中，仅记录 `RealLLMDecisionModel.build_payload` 的安全结构摘要：

| 模型调用 | 用户可见上下文字段 | Observation 数量 | Evidence 数量 | 是否含旧 `facts` 字段 |
| --- | --- | ---: | ---: | --- |
| 第一次 | `user_query`, `available_tools`, `observations`, `evidence` | 0 | 0 | 否 |
| 第二次 | `user_query`, `available_tools`, `observations`, `evidence` | 1 | 5 | 否 |

这证明真实 `RealLLMDecisionModel` 请求载荷使用 V2-1 的 observations 结构，而非旧 `facts` 字段。

### Evidence Validation

第二轮 ANSWER 的最终已验证 `used_evidence` 为：

```text
get_order_facts.order.status
get_order_facts.team.status
get_order_facts.team.target_count
get_order_facts.team.complete_count
get_order_facts.activity.status
```

其中包含本次验收要求的四项核心路径。所有路径均来自该真实 Observation 的 Evidence，未发生 `MODEL_CONTRACT_MISMATCH` 或 `MODEL_EVIDENCE_NOT_AVAILABLE`。

## C. 当前能力外的退款咨询

输入：`我的退款什么时候到账？`

- 真实模型决策：`HANDOFF`
- 最终状态：`HANDOFF`
- `tool_call_count=0`
- 未产生 Observation、ToolResult 或任何退款 Tool 调用。
- 最终用户可见结果为安全转人工文案；没有编造退款状态、到账时间或平台规则。

## 结论

```text
REAL_LLM_OBSERVATION_CONTEXT = PASS
REAL_JAVA_OBSERVATION_FLOW = PASS
REAL_EVIDENCE_VALIDATION = PASS
UNSUPPORTED_QUERY_SAFETY = PASS
V2_1_LIVE_SMOKE = PASS
```
